import App from '@/App'

import { renderWithRouter } from '@/test-utils'

test('404-page', () => {
	// const { container } = renderWithRouter(<App />, { route: '/something-that-does-not-match' })
	// expect(container.getElementsByTagName('h1')[0]).toHaveTextContent('Oops!')
})
